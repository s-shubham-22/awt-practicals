'use strict';

/*
Create a program that declares and initializes variables of different data
types(string, number, boolean) and displays their values.Write a function that
takes two numbers as parameters and returns their sum.
*/

// Declare and initialize variables
let string = 'Hello World!';
let number = 42;
let boolean = true;

// Display values
console.log(string);
console.log(number);
console.log(boolean);

// Function that takes two numbers as parameters and returns their sum
function sum(a, b) {
  return a + b;
}

// Display sum
console.log(sum(1, 2));
